/**
 * A simple serial connection between two boxes to test bad connections on open
 * ports: The serial connection expects at least one connection.
 */

A = box a( out x open )
B = box b( in x open )
connect A.B

