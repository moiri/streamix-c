/**
 * Use a net top perform a stright serial connection
 * Nothing is used twice
 */

A = box funcA(in a, out b)
B = box funcB(in b, out c)
X = box funcX(out a)
Z = box funcY(in c)

Y = A.B
connect X.Y.Z
