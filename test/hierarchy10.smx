/**
 * Hierarchical wrapper structure with serial connections, no port renaming
 */

B = box B( in b )

wrapper W1( out b ) {
    wrapper W2( out b ) {
        A2 = box A2( out b )
        connect A2
    } net( down out b )
    connect W2
} net( down out b )

connect W1.B
