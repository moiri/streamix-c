L = box L( out p1 )
R = box R( in p2 )
wrapper W { in p1, out p2 } {
    link { this, A }
    link { this, B }
    A = box A ( in p1, out p_int )
    B = box B ( in p_int, out p2 )
    connect A.B
}

connect L.W.R
