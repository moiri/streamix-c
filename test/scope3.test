A = box A( out a )
B = box B( in a, out p )
wrapper W{ in p{ p_int } }{
    link { this, A }
    A = box A( in p_int, out b )
    B = box B( in b, out c )
    wrapper W{ in c{w} } {
        link { this, W }
        W = box W(in w)
        W|W
    }
    A.B.W
}
A.B.W
